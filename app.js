// Add functionality to the switch/toggler button to toggle between light and dark mode using javascript

/* function darkMode() {
  let element = document.body;
  element.classList.toggle("dark-mode");
}
 */

let element = document.body;
const darkMode = () => {
  element.classList.toggle('dark-mode');
}